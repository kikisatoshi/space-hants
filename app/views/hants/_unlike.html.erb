<%= form_for(current_user.hanterships.find_by(hant_id: hant.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= button_tag( class: "btn btn-xs") do %>
    <%= content_tag :span, "", class: "glyphicon glyphicon-thumbs-up red", "aria-hidden"=> "true" %>
  <% end %>
  <div class="like-count"><%= pluralize(hant.liked_users.count, " Like") %></div>
<% end %>
